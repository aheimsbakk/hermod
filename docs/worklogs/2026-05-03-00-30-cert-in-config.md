---
when: 2026-05-03T00:30:48Z
why: Eliminate separate certificate files so config.yaml is the single source of truth for all hermod settings
what: Store TLS cert and key as PEM strings inside config.yaml instead of ~/.hermod/server.crt and ~/.hermod/server.key
model: github-copilot/claude-sonnet-4.6
tags: [tls, config, refactor, security]
---

Replaced `cert_path`/`key_path` file-path fields in `HermodConfig` with `tls_cert`/`tls_key` PEM string fields (`src/hermod/core/config.py`). Rewrote `src/hermod/server/tls.py` so `generate_self_signed` returns `(cert_pem, key_pem)` strings, `get_server_ssl_context` loads from PEM strings via temp files, `get_client_ssl_context` uses `cadata=` in-memory loading, and `fingerprint_sha256` accepts PEM bytes/string directly. Updated `src/hermod/cli/main.py` `serve` command to generate and persist PEM strings into config on first run. Updated `tests/conftest.py` fixtures accordingly. Bumped to v0.3.1.
