---
when: 2026-05-03T00:36:14Z
why: Port was inconsistent across config, signaling server, CLI, and tests — standardise on 8786
what: Normalise default server port to 8786 across all layers
model: github-copilot/claude-sonnet-4.6
tags: [config, consistency, port]
---

Changed the default server port from the mixed `4430`/`8765` values to `8786` uniformly across `src/hermod/core/config.py` (dataclass default and `load_config` dict), `src/hermod/server/signaling.py` (`start()` and `run_server()` signatures), `src/hermod/cli/main.py` (`serve` command default), `tests/test_config.py` (all expected values and test data), and documentation in `BLUEPRINT.md` and `CONTEXT.md`. Bumped to v0.3.2.
